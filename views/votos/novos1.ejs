<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adicionar Voto</title>
</head>
<body>
    <h1>Adicionar Nova Ata</h1>
    <form action="/votos/<%= mesa_id %>/novo?token=<%= token %>" method="POST">
        <label for="mesa">Mesa:</label>
        
        <label for="candidato">Candidato:</label>
        <select id="candidato" name="candidato_id" required>
            <% candidatos.forEach(candidato => { %>
                <option value="<%= candidato.id %>"><%= candidato.nome %></option>
            <% }) %>
        </select>
        
        <label for="quantidade_votos">Quantidade de Votos:</label>
        <input type="number" id="quantidade_votos" name="quantidade_votos" required>
        <button type="submit">Adicionar</button>
    </form><br><br><br>
    <a href="/<%=queryParams%>">Voltar para Inicio</a>
</body>
</html>
  